\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}

%\DeclareMathAlphabet{\mathcal}{OMS}{cmsy}{m}{n}
\DeclareSymbolFont{letters}{OML}{txmi}{m}{it} % Redefines Greek

\usepackage{microtype}
\usepackage{mathtools}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{wasysym}
\usepackage{tikz}
\usepackage{xspace}
\usepackage{enumerate}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{prftree}
\usepackage[noend]{algpseudocode}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{adjustbox}
\usepackage{paralist}
\usepackage{url}
\usepackage{cite}
\usepackage{listings}
\usepackage{prftree}
\usetikzlibrary{automata}

\graphicspath{{icons/}}
\usepackage[hidelinks]{hyperref}

\makeatletter
\def\@citecolor{blue}%
\def\@urlcolor{blue}%
\def\@linkcolor{blue}%
\def\UrlFont{\rmfamily}

%\def\orcidID#1{\relax{\href{https://orcid.org/#1}{\protect\raisebox{-1.25pt}{\protect\includegraphics{ORCID_Color.eps}}}}}
\makeatother

%\newtheorem{definition}{Definition}
%\newtheorem{proposition}[definition]{Proposition}
%\newtheorem{conjecture}[definition]{Conjecture}
\newcommand{\Lex}{\texttt{Lex}}
\newcommand{\kw}[1]{{\color{purple}\texttt{#1}}}

\newcommand{\Oblig}{\Circle}
\newcommand{\Epsilon}{\mathcal{E}}
\newcommand{\Models}{\mathcal{M}}

\linepenalty=1000

\begin{document}

\title{Lex: A Programming Language for Developing Enforceable Legal Formalizations}

\author{Jonas Degelo \and François Hublet \and Jakob Merane}

\authorrunning{J. Degelo et al.}

\institute{ETH Zürich, Switzerland\\\email{\{degeloj,fhublet,jmerane\}@ethz.ch}}

\maketitle

%\vspace*{-2ex}
\begin{abstract}
  \keywords{Legal formalization \and Programming languages.}
\end{abstract}
%

%
\iffalse
\section{Introduction}


\section{Enforcement under refinement}

\newcommand{\Events}[1]{\mathbb{E}(#1)}
\newcommand{\Pow}[1]{\mathcal{P}(#1)}

\begin{definition}
  Let $\Phi=(\Sigma, \kappa, \phi)$ and $\Phi'=(\Sigma',\kappa',\phi')$ be two specifications. We say that $\Phi$ refines $\Phi'$ under assumption $\psi$ iff $\exists \pi: \Events{\Sigma} \to \mathrm{MFOTL}(\Sigma')$ such that
  \begin{enumerate}[(i)]
  \item  $v, i\vDash_{\pi(\sigma)} \phi' \land \psi \Longrightarrow v, i\vDash_{\sigma} \phi$
  \item $\kappa' \vdash \pi(e) : \kappa(e)$
  \end{enumerate}
\end{definition}

\begin{theorem}
If $\Phi$ 
\end{theorem}

\section{Logical foundations}


\Lex{} has its foundations in runtime enforcement~\cite{Schneider2000} and uses
Metric First-Order Temporal Logic~\cite{Chomicki1995,Basin2015} as its underlying logic.
In contrast with other approaches to legal formalization~\cite{...}, its logic is thus `classical,'
having no explicit deontic operators (as in deontic logic) nor obligation-specific syntax
(as in input-output logic). In this section, we outline our runtime-enforcement-based
approach to legal formalization and briefly compare it to deontic and input-output logics.

\subsection{Obligations through the lense of runtime enforcement}

Consider a conditional obligation of the form ``if $a$ is the case, then it ought to be that $b$ is the case,'' where $a,b$ are taken from some object logic, e.g., first-order logic.
Such an obligation can be represented by the formula $\Oblig(a \rightarrow b)$ in standard deontic logic and
by the pair $(a,b)$ in input-output logic. In our framework, we simply represent such an
obligation by the implication $a \rightarrow b$. This implication is passed to an \emph{enforcer},
which is a mapping
$\Epsilon: F \rightarrow \Models \rightarrow \Models \cup \lbrace \bot\rbrace $, where $F$ is the set of formulae in the
object logic and $\Models$ is the set of models, such that
\begin{align*}
  \forall \phi, M.\,(\Epsilon(\phi, M) \neq \bot) \Rightarrow (\Epsilon(\phi,M) \models \phi).
\end{align*}
 When $\phi = a \rightarrow b$ and ..., we obtain $\Epsilon(a \rightarrow b, M) \models a \rightarrow b$

 \fi
\section{Syntax}

\subsection{Modules}

Every \Lex{} file defines a module, while folders containing \Lex{} files define groups of modules. The syntax of module imports is as follows:
\begin{align*}
  import ::=&~\kw{import}~[\kw{formex} \mid \kw{akomaNtoso}]~idents\\
  idents ::=&~ ident \mid idents\kw{.}ident
\end{align*}
The special flags \kw{formex} and \kw{akomaNtoso} instruct the \Lex{} compiler to load the
corresponding modules as XML files containing a machine-readable version of the law, rather than
\Lex{} files. Two formats are supported: FORMEX (the standard EU format for machine-readable
legal documents) and Akoma Ntoso (an international standard used, e.g., in Switzerland).

When the target module is a \Lex{} module, the sequence $ident_1\kw{.}\dots\kw{.} ident_n$ refers
to the file \texttt{$ident_1$/\dots/$ident_n$.lex}; if the target module is an XML module in
FORMEX or Akoma Ntoso format, the file is \texttt{$ident_1$/\dots/$ident_n$.xml}.

\subsection{Labels}

Labels are used to capture the structure of the law. Their syntax is as follows:
\begin{align*}
  label\_level::=&~ \kw{law}\mid \kw{title} \mid \kw{chapter} \mid \kw{section} \\
              &\mid \kw{article} \mid \kw{paragraph} \mid \kw{point} \mid \kw{subpoint}\\
  label ::=&~ label\_level[\kw{[} int\kw{]}]~string~[string]
\end{align*}
The optional integer parameter that follows the level can be used to encode an arbitrary number
of additional hierarchical levels: $\kw{chapter}\kw{[}1\kw{]}$ is one level below $\kw{chapter}$
but still above $\kw{section}$, $\kw{chapter}\kw{[}i\kw{]}$ is one level below
$\kw{chapter}\kw{[}i-1\kw{]}$ and one level above $\kw{chapter}\kw{[}i+1\kw{]}$, etc. The second
and (optional) third argument collect the number and header of the corresponding part of the law.

\begin{example}
  Art. 2 GDPR can be introduced as
  \begin{align*}
    \kw{article}~\kw{"}\text{\itshape 2}\kw{"}~\kw{"}\text{\itshape Material scope}\kw{"}
  \end{align*}
\end{example}

The levels $\kw{law}$, $\kw{article}$, $\kw{paragraph}$, $\kw{point}$, and $\kw{subpoint}$ plain
a special role in determining the \emph{qualified name} of \Lex{} rules. Rules can only defined
within articles, which can themselves only defined within laws. The induced uniqueness
constraints are shown in Table~\ref{tab:uniqueness}. The qualified name of a rule is of the
form
$$law~article[\kw{(}paragraph\kw{)}][\kw{(}point\kw{)}][\kw{(}subpoint\kw{)}]~\kw{\#}rule\_id$$
where additional level numbers placed in brackets can be introduced if intermediate,
indexed levels are present. Rules introduced within the scope of a given label can
be referred to using a suffix of their qualified name that omits the part determined by the
scope. The set of all rules located within the scope of a given label can be referred to using the
prefix of their qualified name corresponding to that scope, or by a suffix of this prefix,
if applicable within the current scope.

\begin{example}
  The set of rules defined in Art. 2, §2 of the GDPR can be referred to as
  $\kw{"}\text{\itshape GDPR~2(2)}\kw{"}$. Within the GDPR, they can be referred to as just
  $\kw{"}\text{\itshape 2(2)}\kw{"}$, and within Art. 2, as $\kw{"}\text{\itshape (2)}\kw{"}$.
  The first of such rules is $\kw{"}\text{\itshape GDPR~2(2)~\#1}\kw{"}$, the second one is
  $\kw{"}\text{\itshape GDPR~2(2)~\#2}\kw{"}$, etc. The rule IDs $1..k$ can be overwritten
  by defining explicit rule IDs (see the syntax of rules below).
\end{example}

\begin{table}
  \centering
  \begin{tabular}{llll}
    \hline
    Label level & Number unique within & Part of qualified name & Required \\
    \hline
    \kw{law} & -- & $\checkmark$ & $\checkmark$ \\
    \kw{title} & \kw{law} \\
    \kw{chapter} & \kw{law}\\
    \kw{section} & \kw{law} \\
    \kw{article} & \kw{law} & $\checkmark$ & $\checkmark$ \\
    \kw{paragraph} & \kw{article} & $\checkmark$ \\
    \kw{point} & \kw{paragraph} & $\checkmark$ \\
    \kw{subpoint} & \kw{point} & $\checkmark$ \\
    \hline
  \end{tabular}
  \vspace{10pt}
  \caption{Uniqueness constraints for labels\label{tab:uniqueness}}
\end{table}



\subsection{Types}

\Lex{} has four base types: \kw{int} (integers), \kw{float} (floating-point numbers), \kw{bool} (booleans), and \kw{str} (strings).
New types can be declared as follows:
\begin{align*}
  type\_decl &::= \kw{type}~ident~[[\kw{is}|\kw{subtype}]~ident]
\end{align*}
When another, alreadly existing type name is specified after the \kw{is} keyword, the newly declared type is created as a copy of that existing type. As a consequence, the new type will have the same internal representation and take the same values as the existing type. However, the existing type and the new type will still be considered distinct types by \Lex{}'s type system (see example below). In contrast, the \kw{subtype} keyword support specifying a new type as a subtype of an existing type. In this case, the new type will have both the same internal representation and the capacity to be used in any context where the existing type could be used.

\begin{example}
  Consider the following declarations:
  \begin{align*}
    &\kw{type}~\texttt{id}~\kw{is}~\kw{string}\\
    &\kw{type}~\texttt{user\_id}~\kw{subtype}~\texttt{id}
  \end{align*}
  Further, consider an event with signature
  $\texttt{A}(\texttt{b}\kw{:}\texttt{id}, \texttt{c}\kw{:}\kw{string})$
  and a variable \texttt{uid} of type \texttt{user\_id}. The expression
  $\texttt{A}(\texttt{uid},\kw{"foo"})$ is type-correct, since \texttt{user\_id}
  is a subtype of \texttt{id}. The expression $\texttt{A}(\texttt{uid},\texttt{uid})$ is
  not type-correct, since \texttt{user\_id} is not a subtype of \kw{string}
  (because \texttt{id} is not a subtype of \kw{string}). On the other hand,
  $\texttt{A}(\kw{"foo"},\kw{"foo"})$ is type-correct since the type \texttt{id} can
  contain string values.
\end{example}

\subsection{Events}

The syntax for event declarations is as follows:
\begin{align*}
  capabilities ::=&~\kw{observable} \mid \kw{suppressable} \mid \kw{causable} \mid \kw{internal}\\
                &\mid \kw{observable}~\kw{causable} \mid \kw{causable}~\kw{observable} \\
                &\mid \kw{suppressable}~\kw{causable} \mid \kw{causable}~\kw{suppressable} \\
  event\_decl ::=&~ capabilities~\kw{event}~ident~ \\
                 &\qquad~ [\kw{"""}event\_desc\kw{"""}]\\
                 &\qquad ident_{11}~\kw{:}~ident_{12}\\
                 &\qquad \dots\\
                 &\qquad ident_{k1}~\kw{:}~ident_{k2}
\end{align*}
where $event\_desc$ is a string that can contain special placeholders $\kw{\{}ident_{i1}\kw{\}}$
for $1 \leq i \leq k$. The capabilities associated to an event are organized into the following lattice, where $a \rightarrow b$ denotes that $a$ is strictly weaker than $b$.
\begin{figure}
  \centering
  \begin{tikzpicture}
    \node[text depth=0.1cm] (itl) at (1,1) {\kw{internal}};
    \node[text depth=0.1cm] (cau_sup) at (1,0) {\kw{causable suppressable}};

    \node[text depth=0.1cm] (cau_obs) at (-1,-1) {\kw{causable observable}};
    \node[text depth=0.1cm] (sup) at (3,-1) {\kw{suppressable}};

    \node[text depth=0.1cm] (obs) at (1,-2) {\kw{observable}};
    \node[text depth=0.1cm] (cau) at (-3,-2) {\kw{causable}};

    \node[text depth=0.1cm] (bot) at (-1,-3) {$\bot$};
    \draw[->, dashed] (cau_sup) -- (itl);
    \draw[->] (sup) -- (cau_sup);
    \draw[->] (cau_obs) -- (cau_sup);
    \draw[->] (cau) -- (cau_obs);
    \draw[->] (obs) -- (cau_obs);
    \draw[->] (obs) -- (sup);
    \draw[->] (bot) -- (obs);
    \draw[->] (bot) -- (cau);
  \end{tikzpicture}
  \caption{Capability lattice}
\end{figure}

The pairs of identifiers $ident_{i1}\kw{:} ident_{i2}$ contained in the event declaration determine an (ordered) list of event parameters ($ident_{i1}$) and their types ($ident_{i2}$). Finally, the event description $event\_desc$ provides the high-level semantics of the event, which should refer to the parameters $ident_{i1},\dots,ident_{k1}$ using the $\kw{\{} ident_{i1}\kw{\}}$ syntax.
\begin{example}
  The following code declares a new suppressable event capturing personal data processing in a system, assuming three previously defined types \texttt{processing\_id}, \texttt{processor\_id}, and \texttt{data\_id}.
  \begin{align*}
    &\kw{suppressable}~\kw{event}~\texttt{PersonalDataProcessing}\\
    &\qquad \kw{"""}\\
    &\qquad \text{\itshape processor \kw{\{}x\kw{\}} is processing personal data \kw{\{}z\kw{\}} as part}\\
    &\qquad \text{\itshape of processing operation \kw{\{}ep\kw{\}}}\\
    &\qquad \kw{"""}\\
    &\qquad\begin{array}{lll}
             \texttt{ep}&\kw{:}&\texttt{processing\_id}\\
             \texttt{x}&\kw{:}&\texttt{processor\_id}\\
             \texttt{z}&\kw{:}&\texttt{data\_id}
           \end{array}
  \end{align*}
\end{example}

\subsubsection{Event capabilities}
There are two classes of event capabilities: \kw{internal} and all other capabilities.
Choosing a capability (other than internal) depends on two factors:
1. whether the event can actually be observed, caused, suppressed, etc. and
2. what the enforcer requires as a "minimal" capacity in order for the rules where the event is used to be enforced.

\newpage
\kw{observable}
\begin{itemize}[*]
  \item The event can be observed by the enforcer.
  \item e.g. an event might not be observable if it is not always logged when it happens (this is highly dependent on the system in question).
\end{itemize}

\kw{causable} 
\begin{itemize}[*]
  \item The event can be caused by the enforcer.
\end{itemize}

\kw{suppressable}
\begin{itemize}[*]
  \item The event can be suppressed by the enforcer. (every occurence of the event is logged)
  \item \kw{suppressable} events are \kw{observable} by definition. (an event cannot not be suppressed if it was never observed)
\end{itemize}

\kw{observable causable} or \kw{causable observable}
\begin{itemize}[*]
  \item combines the capabilities of \kw{observable} and \kw{causable}
\end{itemize}

\kw{suppressable causable} or \kw{causable suppressable}
\begin{itemize}[*]
  \item The event can be suppressed and caused by the enforcer.
  \item This capability inherits observability from \kw{suppressable}
\end{itemize}

\kw{internal} events serve a special purpose in the \Lex{} language.
\begin{itemize}[*]
  \item They occur on the right-hand side of constitutive rules (after the \kw{constitute} keyword).
  \item They are not "emmitted" by the system, rather they can be seen as placeholders for a combination of other events, as defined by constitutive rules.
  \item Completely hidden from the programmer, they are also used to internally create special events for scope and exception rules.
\end{itemize}

\subsection{Rules}

The general syntax for rule declarations is as follows:
\begin{align*}
  % rule\_verb\_action ::=&~\kw{oblige} \mid \kw{permit} \mid \kw{prohibit} \\ % TODO: allow for different action verbs (permit, prohibit)
  rule\_verb\_action ::=&~\kw{oblige} \\
  rule\_verb\_constitute ::=&~\kw{constitute} \\
  rule\_verb\_relative ::=&~\kw{except} \mid \kw{scope}\\
  rule\_constr\_verb ::=&~\kw{causing} \mid \kw{suppressing}\\
  rule\_constr ::=&~rule\_constr\_verb~ident_1~\dots~ident_m\\
  rule\_action ::= &~rule\_verb\_action~[patt_2]\\
                   &\qquad [ident_{41}\kw{:}]~expr_{41}\\
                   &\qquad \dots\\
                   &\qquad [ident_{4{k_4}}\kw{:}]~expr_{4{k_4}}\\
  rule\_reference ::= &~\kw{\{}~
                      label\_level_1[\kw{[}int_1\kw{]}]~string_1
                      ~\dots~
                      label\_level_k[\kw{[}int_k\kw{]}]~string_{k}
                      ~[\kw{rule}~string_{k+1}]
                      ~\kw{\}} \\
  rule\_relative ::=&~rule\_verb\_relative~[patt_2] \\
                    &~\qquad rule\_reference_1 \\
                    &~\qquad\dots \\
                    &~\qquad rule\_reference_l \\
  rule\_decl ::=&~\kw{rule}~[string]\\
                &~[\kw{fix}\\
                &\qquad ident_{11}\kw{:} ident_{21}\\
                &\qquad \dots\\
                &\qquad ident_{1n}\kw{:} ident_{2n}]\\
                &~[\kw{whenever}~[patt_1]\\
                &\qquad [ident_{31}\kw{:}]~expr_{31}\\
                &\qquad \dots\\
                &\qquad [ident_{3{k_3}}\kw{:}]~expr_{3{k_3}}]\\
                &~(rule\_action\mid rule\_relative)\\
                &[[\kw{transparently}]~\kw{enforceable}]~rule\_constr_1\kw{,}\dots\kw{,}rule\_constr_\ell
\end{align*}
In the following we review the syntax of expressions $expr_{ij}$ (Section~\ref{sec:expr}), the available verbs (Section~\ref{sec:expr-verb}) and the temporal patterns determined by $patt_1$ and $patt_2$ (Section~\ref{sec:opt}).

\subsubsection{Expressions.}

\label{sec:expr}

The syntax of expressions closely follows MFOTL syntax:
\begin{align*}
  expr\_itv ::= &~ \kw{[}time\_bound_1\kw{,}(time\_bound_2\mid \kw{*})\kw{]}\\
  term ::= &~(const \mid ident) [\kw{:} ident]\\
  pred ::= &~ident\kw{(}term_1\kw{,}\dots\kw{,}term_k\kw{)}\\
  expr ::= &~ \kw{TRUE} \mid \kw{FALSE} \mid pred\\% \mid special\_pred \\
  \mid &~ \kw{NOT}~expr \mid expr_1~(\kw{AND} \mid \kw{OR} \mid \kw{IMPLIES} \mid \kw{EQUIV})~expr_2 \\
  \mid &~ (\kw{EXISTS} \mid \kw{FORALL})~expr \\
  \mid &~(\kw{ONCE} \mid \kw{EVENTUALLY} \mid \kw{HISTORICALLY} \mid \kw{ALWAYS})~[expr\_itv]~expr\\
  \mid &~expr_1~(\kw{SINCE} \mid \kw{UNTIL} \mid \kw{TRIGGER} \mid \kw{RELEASE})~[expr\_itv]~expr_2%\\
  %special\_pred ::= &~ \kw{obliged}(pred) \mid \kw{permitted}(pred) \mid \kw{prohibited}(pred) \\
  %\mid&~\kw{applicable}(string) \mid \kw{satisfied}(string) \mid \kw{violated}(string) 
\end{align*}

\subsubsection{Verbs.}

\label{sec:expr-verb}

\subsubsection{Temporal patterns.}

\label{sec:opt}

Except for $\kw{except}$, $\kw{scope}$, and $\kw{constitute}$ rules, % TODO: possibly rewrtie as only for $\kw{oblige}$ rules (depending on whether or not permit/prohibit rules are implemented)
every \Lex{} rule declaration represents a statement of the form
\begin{align*}
  &\kw{FORALL}~ident_{11},\dots,ident_{1n}. \\
  &\quad\kw{ALWAYS} (
    form_1 \left(expr_{11}~\kw{AND}~\ldots~\kw{AND}~expr_{1k}\right)\\
    &\qquad \kw{IMPLIES}~
    form_2 \left( expr_{21}~\kw{AND}~\ldots~\kw{AND}~expr_{2k} \right)
  )
\end{align*}
where $form_1$ and $form_2$ are determined by $patt_1$ and $patt_2$, respectively. These options use the following
grammars for time and time intervals:
\begin{align*}
  time\_unit ::= &~\kw{y} \mid \kw{d} \mid \kw{h} \mid \kw{s}  \\
  time\_bound ::= &~ int~time\_unit \\
  time\_interval ::= &~ \kw{within}~time\_bound \mid \kw{before}~time\_bound \mid \kw{after}~time\_bound
\end{align*}

Table~\ref{tab:patterns} lists available $patt_i$ and the corresponding $form_i$ functions, as
well as the conversion of $time\_interval$ expressions into intervals:

\begin{table}
  \centering
  \begin{tabular}{lll}
    \hline
    $itv$ & $I$ & Restriction \\
    \hline
    -- & $[0,\infty)$\\
    $\kw{within}~b$ & $[0,b]$ \\
    $\kw{before}~a$ & $[a,\infty)$ & Only past patterns \\
    $\kw{strictly before}~a$ & $(a,\infty)$ & Only past patterns \\
    $\kw{after}~a$ & $[a,\infty)$ & Only future patterns \\
    $\kw{strictly after}~a$ & $(a,\infty)$ & Only future patterns \\
    $\kw{between}~a~\kw{and}~b$ & $[a,b]$ \\
    $\kw{strictly between}~a~\kw{and}~b$ & $(a,b)$ \\
    $\kw{between}~a~\kw{and}~b~\kw{excluded}$ & $[a,b)$ \\
    $\kw{between}~a~\kw{excluded and}~b$ & $(a,b]$ \\
  \end{tabular}\\[10pt]
    \begin{tabular}{llll}
    \hline
  Description & $patt_i$ & $form_i(\phi)$ & Type \\
    \hline
              & -- & $\phi$ & Present\\
              & $\kw{eventually}~[itv]$ & $\kw{EVENTUALLY}_I~\phi$ & Future\\
              & $\kw{eventually delaying if}~\psi~[itv]$ & $\psi~\kw{UNTIL}_I~\phi$ & Future\\
              & $\kw{always in the future}~[itv]$ & $\kw{ALWAYS}_I~\psi$ & Future\\
              & $\kw{once}~[itv]$ & $\kw{ONCE}_I~\psi$ & Past\\
              & $\kw{always in the past}~[itv]$ & $\kw{HISTORICALLY}_I~\psi$ & Past\\
              & $\kw{always since}~\psi~[itv]$ & $~\phi~\kw{SINCE}_I~\psi$ & Past\\ % TODO: check if this pattern is correct
\end{tabular}\\[10pt]
\caption{Available temporal patterns for \Lex{} rules\label{tab:patterns}}
\end{table}

\textbf{Typing Rules of Temporal Patterns}

\begin{figure}
  \begin{gather*}
    \prftree[r]
      {\text{Present}^{\mathbb{C}}}
      {\Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \qquad
    \prftree[r]{\text{Present}^{\mathbb{S}_j}}
      {\Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \bigwedge_{i=0}^n \phi_i : \mathbb{S}}
      \\
    \prftree[r]{\text{Eventually}^{\mathbb{C}}}
      {\Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \kw{EVENTUALLY}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \qquad
    \prftree[r]{\text{Eventually}^{\mathbb{S}_j}}
      {\Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \kw{EVENTUALLY}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{S}}
      \\
    \prftree[r]{\text{Always}^{\mathbb{C}}}
      {\Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \kw{ALWAYS}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \qquad
    \prftree[r]{\text{Always}^{\mathbb{S}_j}}
      {\Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \kw{ALWAYS}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{S}}
      \\
    \prftree[r]{\text{Until}^{\mathbb{C}R}}
      {b\not=\infty \quad \Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \psi~\kw{Until}_{[0,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \qquad
    \prftree[r]{\text{Until}^{\mathbb{S}_j}}
      {\Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \psi~\kw{Until}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{S}}
      \\
    \prftree[r]{\text{Until}^{\mathbb{C}LR}}
      {b\not=\infty \quad \Gamma \vdash \psi :\mathbb{C} \quad \Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \psi~\kw{Until}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \\
     \prftree[r]{\text{Historically}^{\mathbb{S}_j}}
      {\Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \kw{HISTORICALLY}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{S}}
      \\
    \prftree[r]{\text{Once}^{\mathbb{C}}}
      {0 \in [a,b] \quad \Gamma \vdash \phi_0 :\mathbb{C} \quad\dots\quad \Gamma \vdash \phi_n : \mathbb{C}}
      {\Gamma \vdash \kw{Once}_{[a,b]}~\bigwedge_{i=0}^n \phi_i : \mathbb{C}}
      \qquad
    \prftree[r]{\text{Since}^{\mathbb{C}}}
      {0 \in [a,b] \quad \Gamma \vdash \psi : \mathbb{C}}
      {\Gamma \vdash \left(\bigwedge_{i=0}^n~\phi_i\right)~\kw{Since}_{[a,b]}~\psi : \mathbb{C}}
      \\
    \prftree[r]{\text{Since}^{\mathbb{S}L_j}}
      {0 \not\in [a,b] \quad \Gamma \vdash \phi_j : \mathbb{S}}
      {\Gamma \vdash \left(\bigwedge_{i=0}^n~\phi_i\right)~\kw{Since}_{[a,b]}~\psi : \mathbb{S}}
      \qquad
    \prftree[r]{\text{Since}^{\mathbb{S}LR_j}}
      {0 \in [a,b] \quad \Gamma \vdash \phi_j : \mathbb{S} \quad \Gamma \vdash \psi : \mathbb{S}}
      {\Gamma \vdash \left(\bigwedge_{i=0}^n~\phi_i\right)~\kw{Since}_{[a,b]}~\psi : \mathbb{S}}
  \end{gather*}
  \caption{Typing rules for temporal patterns}
\end{figure}

\section{Compilation}

\subsection{To MFOTL}

\subsection{To LegalRuleML}

% LegalRuleML: Design Principles and Foundations

\subsection{To documentation}

% Temporal patterns: https://dl.acm.org/doi/pdf/10.1145/1062455.1062526

% todo:

% finish module system in enforceability step → Jonas
% refinement → Jonas
% VS Code → Jonas
% modify... constitute  → Jonas
% except/modify ... other verb? → Jonas

% design survey
% other verbs, including permit
% isobliged etc. → medium

% fix shift/reduce and reduce/reduce conflicts
% localization

{%\setlength\bibsep{2pt}
\small
\bibliographystyle{unsrt}
\bibliography{lex}
}
 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End
