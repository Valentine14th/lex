(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (explain true))

(library
 (name lex_lib)
 (libraries core core_unix core_unix.sys_unix re xml-light calendar z3 MFOTL_lib)
 (preprocess
  (pps ppx_jane)))
